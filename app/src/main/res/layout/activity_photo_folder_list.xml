<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout style="@style/page"
                xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
    >

    <include layout="@layout/include_top_title_layout"
        android:id="@+id/top_layout_folder_list"
        />

    <View
        android:id="@+id/sepLine"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_below="@+id/top_layout_folder_list"
        android:background="#999"/>

    <ListView
        android:id="@+id/listView"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@+id/sepLine"
        android:layout_marginTop="0dp"
        android:scrollbars="none"
        android:cacheColorHint="#00000000"
        android:divider="@drawable/hor_sepline"
        android:dividerHeight="2dp"
        android:fadingEdge="none"
        android:listSelector="#00000000"/>

</RelativeLayout>